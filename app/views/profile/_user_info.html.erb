<div id="<%= "#{dom_id(user)}_user_info" %>" >
<%= turbo_frame_tag dom_id(user) do %>
    </section>
    <section class="m-auto md:w-10/12 flex justify-between my-4 py-4 items-center ">
        <h3 class="text-2xl w-100 font-semibold text-gray-800"><%= user.name.titleize %></h3>
        <% if user.rating %>
        <p class="font-bold text-lg">⭐<%= user.rating.rating %></p>
        <% end %>
    </section>
<% end %>
</div>
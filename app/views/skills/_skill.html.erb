
<div id="<%= "#{dom_id(skill)}_content" %>" >
<%= turbo_frame_tag dom_id(skill) do %>
<div  class="text-lg bg-green-300 px-4 py-1 w-24 rounded-full text-center mr-2 font-semibold text-gray-800 shadow-lg my-2 flex items-center justify-evenly">
  <section>
    <%= skill.name.titleize %>
  </section>
  <% if session[:user_id].to_i == skill.user.id then %>
  <section>
  <%= link_to "×" , user_skill_path(skill.user,skill) , data: { turbo_method: :delete } %>
  </section>
  <% end %>
</div>
  <% end %>
</div>
<div>
 <div>
        <%= render "home/header" , user: @user %>
    </div>
    
    <div class="w-11/12 m-auto p-4 mt-2 flex
    justify-center">
    
    <%= render "partials/search" , action: "/tasks" %>

    </div>

    <div class="flex flex-col  p-2  md:flex-row w-11/12 m-auto md:m-auto  md:justify-between md:flex-wrap">
         <% @tasks.each do |task| %>
             <%= render "task_card" , task: task , participants: task.participants , user: @user %>
          <% end %>
    </div>
</div>